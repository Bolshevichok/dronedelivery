{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "mission-service",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}/cmd/mission/main.go",
            "env": {
                "configPath": "${workspaceFolder}/config.yaml"
            }
        }
    ]
}